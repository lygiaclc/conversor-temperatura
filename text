#Conversor de temperatura:Crie um site que peça ao usuário a temperatura em graus Celsius (°C), utilizando prompt(), e devolva, no console, a temperatura em graus Farenheit (°F) utilizando console.log(). A fórmula matemática para converter de °C a °F é: T(°F) = ( T(°C) × 1.8 ) + 32

#Tarefas (TODO)
- criar um html
- criar um js;
- estrutura html da page;
- perguntar a temperatura em Celsius (°C)
- converter a temperatura para farenheit (°F)
- mostrar o resultado no console 